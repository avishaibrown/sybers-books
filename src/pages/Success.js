import { Typography } from "@mui/material";

const Success = () => {
  return (
    <Typography>
      You have successfully completed your Stripe payment.
    </Typography>
  );
};

export default Success;
